This folder will contain two versions of the random forest classifier. The rf_classifier works on windows machines and utilizes all available processors. 
The rf_gpu_classifier is only compatible with linux distributions and uses cuml to utilize GPUs to speed up the machine learning process. 
